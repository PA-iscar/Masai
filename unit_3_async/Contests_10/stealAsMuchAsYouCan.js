function runProgram(input){
    input = input.split("\n");
    let C = Number(input[0]);
    let n = Number(input[1]);
    let val = input[2].split(" ").map(Number);
    let w = input[3].split(" ").map(Number);
    let profit = 0;

    for(let i=0;i<n;i++){
        
    }
}

runProgram(`50 
3
60 100 120 
10 20 30`);